1. What are the primary differences between Arrays and ArrayLists?

ArrayLists can expand

2. Why might you use an Array instead of an ArrayList?

If you want to keep the values in a array set and not change them, also to keep the code simpler if an array list is ot needed

3. Why might you use an ArrayList instead of an Array?

If an array needs to be more complicated and expand to work use an array list

4. How are Arrays and ArrayLists similar?

they both are data strucutres
